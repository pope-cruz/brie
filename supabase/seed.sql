-- Fictional local-only identities. Do not use real attendee data.
-- Creates nothing until auth users exist. After signing in as the first owner
-- through /app/sign-in, create additional workspaces from the UI or run the
-- documented operator seed in docs/OPERATIONS.md.
select 1;
